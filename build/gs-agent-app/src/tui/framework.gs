import {
  BOLD,
  Color,
  DIM,
  ESC,
  INVERSE,
  RESET,
  Theme,
  UNDERLINE,
  charWidth,
  chars,
  clearLine,
  clearScreen,
  color,
  colorCode,
  enterAlternateScreen,
  fg,
  hideCursor,
  isColorEnabled,
  leaveAlternateScreen,
  line,
  moveTo,
  padRight,
  repeatText,
  setColorEnabled,
  showCursor,
  stripAnsi,
  styleText,
  truncateToWidth,
  visibleWidth,
} from "@/tui/ansi";
import { parseKeys } from "@/tui/keys";
import { Box, Container, Input, Loading, Markdown, Spacer, Text } from "@/tui/components";
import { compactLoading, loadingFrame, loadingText } from "@/tui/loading";
import { runTuiApp } from "@/tui/runtime";
import {
  banner,
  border,
  clampScroll,
  joinColumns,
  renderLines,
  scrollTitle,
  splitLines,
  takeLine,
  viewportLines,
  wrapText,
  wrapTextLine,
} from "@/tui/widgets";

// GoScript 当前还不支持 `export { x } from "..."`，因此这里显式聚合再导出。
export {
  BOLD,
  Box,
  Color,
  Container,
  DIM,
  ESC,
  Input,
  INVERSE,
  Loading,
  Markdown,
  RESET,
  Spacer,
  Text,
  Theme,
  UNDERLINE,
  banner,
  border,
  charWidth,
  chars,
  clampScroll,
  clearLine,
  clearScreen,
  color,
  colorCode,
  compactLoading,
  enterAlternateScreen,
  fg,
  hideCursor,
  isColorEnabled,
  joinColumns,
  leaveAlternateScreen,
  line,
  loadingFrame,
  loadingText,
  moveTo,
  padRight,
  parseKeys,
  renderLines,
  repeatText,
  runTuiApp,
  scrollTitle,
  setColorEnabled,
  showCursor,
  splitLines,
  stripAnsi,
  styleText,
  takeLine,
  truncateToWidth,
  viewportLines,
  visibleWidth,
  wrapText,
  wrapTextLine,
};
